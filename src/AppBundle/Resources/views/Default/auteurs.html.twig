{#Lier la vue au template de page #}
{% extends "@App/Default/base.html.twig" %}

{% block page %}
    <section class="post_review">



        {% for auteur in auteurs %}
            <a class="apercu" href="{{ path('auteur', {'id': auteur.id}) }}">
                <article>
                    <h2>
                        {{ auteur.name }}
                    </h2>
                    <p>
                    {{ auteur.bio }}
                    </p>
                    <p>
                        <span>
                            naissance: {{ auteur.birthdate|date("j - M - Y") }} <br>décès: {{ auteur.deathdate|date("j - M - Y") }}
                        </span>
                    </p>

                </article>
            </a>

        {% endfor %}
    </section>

{% endblock %}